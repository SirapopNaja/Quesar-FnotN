import axios from '../axios'
import {make} from 'vuex-pathify'

const state = {

}

const getter = {

}

const mutation = make.mutations(state)

const action ={

}


export default {
    namespaced :true,
    state,
    getter,
    mutation,
    action,
}